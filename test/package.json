{
  "name": "csdlpt-e2e-test",
  "version": "1.0.0",
  "private": true,
  "description": "E2E test folder — Playwright + HTTP test, độc lập với APP_NGANHANG.",
  "scripts": {
    "test:http": "node e2e_http.js",
    "test:pw": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
